import { LanguageChoosePipe } from './language-choose.pipe';

describe('LanguageChoosePipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageChoosePipe();
    expect(pipe).toBeTruthy();
  });
});
